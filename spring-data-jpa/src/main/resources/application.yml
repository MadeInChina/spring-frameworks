spring:
  datasource:
    url: jdbc:hsqldb:mem:test_jpa
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: org.hsqldb.jdbcDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.HSQLDialect
    database: hsql